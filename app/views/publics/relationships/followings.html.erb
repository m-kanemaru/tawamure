<div class="container" >
  <div class="row">
    <div class="col h2 mt-3">
      <%= link_to @user.nickname, @user %>さんのフォロワー一覧
    </div>
    
    <% if @users.exists? %>
      <%= render 'publics/users/index', users: @users %>
    <% else %>
      <p>ユーザーはいません</p>
    <% end %>
  </div>
</div>