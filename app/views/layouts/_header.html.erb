<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-success">
        <div class="container">
            <a class="navbar-brand" href="/">
              <span class="text-light">TAWAMURE</span>
            </a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNavDropdown">
                <ul class="navbar-nav ml-auto">
                        <li>
                            <%= link_to '投稿', posts_path,class: 'nav-link text-light' %>
                        </li>
                        <li>
                            <%= link_to 'カテゴリ', groups_path,class: 'nav-link text-light' %>
                        </li>
                        <li>
                            <%= link_to 'ユーザー', users_path,class: 'nav-link text-light' %>
                        </li>
                    <% if user_signed_in? %>
                        <li>
                            <%= link_to 'マイページ', user_path(current_user),class: 'nav-link text-light' %>
                        </li>
                            <%= render 'searches/search' %>
                    <% else %>
                        <li>
                            <%= link_to 'ログイン', new_user_session_path,class: 'nav-link text-light' %>
                        </li>
                        <li>
                            <%= link_to '登録', new_user_registration_path,class: 'nav-link text-light' %>
                        </li>
                    <% end %>
                </ul>
            </div>
        </div>
    </nav>
</header>